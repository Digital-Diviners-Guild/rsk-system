{{#if dealsDamage}}
<div>
    <strong>{{localize "RSK.Damage"}}: </strong>
    {{#each system.damageEntries as |damage type|}}
    {{!-- todo: how to localize? --}}
    {{#if damage}}
    <span>{{damage}} ({{type}}) </span>
    {{/if}}
    {{/each}}
</div>
{{/if}}