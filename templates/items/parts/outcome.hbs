<div data-group="{{group}}">
    <div class="form-group">
        <strong>{{localize "RSK.ConfiguredOutcomes"}}</strong>
        <ul class="target-state-changes-list">
            {{!-- todo: what was changes in our lab project? --}}
            {{#each changes as |change index|}}
            <li class="operation" data-index="{{index}}">
                <span>{{change.displayParams}}</span>
                {{!-- todo: use our remove trash can icon --}}
                <button type="button" class="delete-operation" data-index="{{index}}">Delete</button>
            </li>
            {{/each}}
        </ul>
    </div>

    <div>
        <strong>{{localize "RSK.AddOutcome"}}</strong>
        <div class="form-group">
            <label>{{localize "RSK.Outcome"}}
                <select class="new-outcome">
                    <option value="">{{localize "RSK.SelectOutcome"}}</option>
                    <option value="restoreLifePoints">{{localize "RSK.RestoreLifePoints"}}</option>
                    <option value="receiveDamage">{{localize "RSK.ReceiveDamage"}}</option>
                    <option value="addStatuses">{{localize "RSK.AddStatuses"}}</option>
                    <option value="removeStatuses">{{localize "RSK.RemoveStatuses"}}</option>
                </select>
            </label>
        </div>

        <div class="form-group amount-input outcome-input" style="display:none;">
            <label>{{localize "RSK.Amount"}}:
                <input type="number" id="outcome-amount" value="1" min="1">
            </label>
        </div>
        {{!-- todo: we have a statuses input somewhere already we should use --}}
        <div class="form-group status-input outcome-input" style="display:none;">
            <label>{{localize "RSK.Status"}}:
                <input type="text" id="outcome-status">
            </label>
        </div>

        {{!-- todo: wonder if this can replace the 'damage entries' in the model, and those could exist on a sheet for
        display, but
        configured through outcomes. the benefit would be one way to input damage regardless of the scenario, but for
        viewing in the context
        of a weapon it could still be shown in a way that makes sense. ie the way damage entires is shown now. --}}
        <div class="form-group damage-input outcome-input" style="display:none;">
            <div class="form-group">
                <label>{{localize "RSK.Typeless"}}:
                    <input type="number" class="damage-input-typeless" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Stab"}}:
                    <input type="number" class="damage-input-stab" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Slash"}}:
                    <input type="number" class="damage-input-slash" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Crush"}}:
                    <input type="number" class="damage-input-crush" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Air"}}:
                    <input type="number" class="damage-input-air" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Fire"}}:
                    <input type="number" class="damage-input-fire" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Water"}}:
                    <input type="number" class="damage-input-water" value="0" min="0">
                </label>
            </div>
            <div class="form-group">
                <label>{{localize "RSK.Earth"}}:
                    <input type="number" class="damage-input-earth" value="0" min="0">
                </label>
            </div>
        </div>
        <button type="button" class="add-outcome">{{localize "RSK.AddOutcome"}}</button>
    </div>
</div>